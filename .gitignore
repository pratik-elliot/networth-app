node_modules/
server/data/
server/uploads/*
!server/uploads/.gitkeep
server/.env
client/dist/
.DS_Store

# Guard against DB_PATH/UPLOAD_DIR resolving to the repo root — uploaded
# statements and account documents must never be committed.
/data/
/uploads/
.env
